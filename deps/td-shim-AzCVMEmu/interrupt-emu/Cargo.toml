[package]
name = "interrupt-emu"
version = "0.1.0"
edition = "2021"
license = "BSD-2-Clause-Patent"

[lib]
name = "interrupt_emu"
path = "src/lib.rs"

[dependencies]
lazy_static = { version = "1.4", features = ["spin_no_std"] }
spin = "0.9"
log = "0.4"
