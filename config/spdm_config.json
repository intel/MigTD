{
    "__usage": "This helps generate compile-time constant sizes for SPDM library when spdm_attestation is used. sender_buffer_size/receiver_buffer_size should be aligned to io device buffer size (current equal to DEFAULT_BUF_SIZE to support virtio serial).",
    "cert_config": {
        "max_cert_chain_data_size": 4096
    },
    "measurement_config": {
        "max_measurement_record_size": 4000,
        "max_measurement_val_len": 1024
    },
    "psk_config": {
        "max_psk_context_size": 64,
        "max_psk_hint_size": 32
    },
    "max_opaque_list_elements_count": 1,
    "max_session_count": 1,
    "transport_config": {
        "sender_buffer_size": 4096,
        "receiver_buffer_size": 4096
    },
    "max_spdm_msg_size": 65536,
    "heartbeat_period_value": 0,
    "max_root_cert_support": 0
}
